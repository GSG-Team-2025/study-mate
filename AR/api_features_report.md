# 📘 تقرير شامل عن مشروع Study Mate (السيناريوهات والـ APIs)

بناءً على التحليل الدقيق لقاعدة البيانات (Database Schema) والـ APIs التي تم بناؤها، هذا هو الوصف الكامل للمشروع والوظائف المتاحة حالياً للمبرمجين (Frontend Developers).

---

## 🌟 نظرة عامة (The Vision)
مشروع **Study Mate** هو منصة تعليمية شاملة تدمج بين **الحياة الجامعية الأكاديمية** و **تطوير المسار المهني**، مغلفة بنظام **تحفيز (Gamification)** لزيادة إنتاجية الطالب.

---

## 🏛️ 1. القسم الأكاديمي (University Hub)
**الهدف:** مساعدة الطالب في تنظيم مواده الجامعية ومشاركة المصادر.

### السيناريوهات المدعومة:
1.  **عرض المواد (Browse Subjects):**
    *   يستطيع الطالب تصفح جميع مواد الجامعة المتوفرة في النظام.
    *   **الرابط:** `GET /api/subjects`
2.  **تسجيل مادة (Enrollment):**
    *   يختار الطالب مادة (مثل CS101) ويضيفها إلى جدوله الدراسي.
    *   **الرابط:** `POST /api/subjects/:id/enroll`
3.  **مكتبة المصادر (Resources Bank):**
    *   عند الدخول لصفحة المادة، يجد الطالب ملفات (PDFs، سلايدات، امتحانات سابقة) رفعها زملاؤه.
    *   **الرابط:** `GET /api/resources?subjectId=...`
4.  **المشاركة ورفع الملفات:**
    *   يستطيع الطالب رفع ملف مفيد لزملائه (ويكسب نقاط XP!).
    *   **الرابط:** `POST /api/resources`

---

## 🚀 2. المسار المهني (Learning Hub)
**الهدف:** تأهيل الطالب لسوق العمل من خلال مسارات وكورسات منظمة.

### السيناريوهات المدعومة:
1.  **اختيار المسار (Roadmaps):**
    *   يستعرض الطالب المسارات المتاحة (Frontend, AI, CyberSecurity).
    *   يفتح تفاصيل المسار ليرى الكورسات المطلوبة بالترتيب.
    *   **الروابط:** `GET /api/roadmaps` و `GET /api/roadmaps/:id`
2.  **دراسة الكورسات (Courses):**
    *   يبدأ الطالب كورس معين، ويظهر له المحتوى (دروس، فيديوهات).
    *   **الرابط:** `GET /api/courses/:id`
3.  **إنجاز الدروس (Progress & XP):**
    *   عند إنهاء درس، يضغط "Mark as Complete".
    *   **النتيجة:** النظام يسجل التقدم ويمنحه نقاط خبرة (XP) فوراً.
    *   **الرابط:** `POST /api/lessons/:id/complete`

---

## ✅ 3. الإنتاجية والمهام (Productivity)
**الهدف:** تنظيم وقت الطالب وربط المهام بالدراسة.

### السيناريوهات المدعومة:
1.  **إدارة المهام (To-Do List):**
    *   يضيف الطالب مهام يومية (مثل: "حل الواجب الأول").
    *   يمكن ربط المهمة بمادة جامعية معينة لتظهر في صفحتها.
    *   **الرابط:** `POST /api/tasks`
2.  **متابعة الإنجاز:**
    *   يقوم الطالب بالتعليم على المهمة أنها انتهت (Check ✅).
    *   **الرابط:** `PATCH /api/tasks/:id` (يرسل `is_completed: true`).

---

## 👤 4. الملف الشخصي والهوية (Profile & Gamification)
**الهدف:** بناء هوية رقمية للطالب تعكس إنجازاته.

### السيناريوهات المدعومة:
1.  **بيانات الطالب الشاملة:**
    *   يعرض النظام: الاسم، الجامعة، المعدل (Tawjihi/GPA)، المسار الحالي، ومستوى الخبرة (Level & XP).
    *   **الرابط:** `GET /api/profile`
2.  **تحديث البيانات (Profile Edit):**
    *   يستطيع الطالب تعديل كل شيء:
        *   الصورة الشخصية (Avatar).
        *   روابط السوشيال ميديا (LinkedIn, GitHub).
        *   بيانات التوجيهي والمعدل.
        *   تغيير المسار الحالي (Switch Roadmap).
    *   **الرابط:** `PUT /api/profile` (يدعم كل الحقول: `tawjihi_year`, `current_roadmap_id`, إلخ).

---

## 🛠️ ملخص تقني للمبرمجين
*   جميع الـ APIs محمية وتتطلب تسجيل الدخول (ما عدا الـ Public GETs أحياناً).
*   البيانات تعود دائماً بصيغة موحدة: `{ data: ..., meta: ..., error: null }`.
*   نظام الصلاحيات (RLS) جاهز ويسمح للطالب بتعديل بياناته فقط (Own Data).

**الخلاصة:**
النظام الآن "Backend-Ready" بنسبة 100% لربط واجهات التطبيق (Mobile/Web) وتحقيق رؤية Study Mate الطموحة. 🚀
